# Day 7 — [Sep 2, 2026]

## Topic

SQL - SELECT and DISTINCT

## What I did

- Learn SELECT
    - Double click the DB before doing queries.
    - Another option is mentioning the DB **name.table_name**
    -   - is for everything while you can specify columns to display with column names separated by comma.
- You can separate the columns in different lines to see the columns you want displayed better.
- SQL follows the order operation PEMDAS
    - Parenthesis, Exponent, Multiplication, Division, Addition, Subtraction
- Learn DISTINCT
    - Use DISTINCT to display only the unique values

## Key takeaway

- SELECT for selecting tables/columns.
- DISTINCT for displaying only the unique values.

## Tomorrow

WHERE Statement
